# /etc/cron.d/document-search-cronjobs
*/5 * * * * datamade {VENV_DIR}/bin/python {PROJECT_DIR}/manage.py send_expiration_email
# You need a newline at the end of your crontab, or cron ignores the file.
